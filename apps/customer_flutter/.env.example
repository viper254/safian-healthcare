# This Flutter app stays disconnected unless these staging-only values are passed at build time.
# Never include a Supabase service-role key, M-Pesa secret, or other privileged credential.
SUPABASE_URL=https://your-staging-project.supabase.co
SUPABASE_PUBLISHABLE_KEY=your_staging_publishable_key
